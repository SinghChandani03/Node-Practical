## To run this project just simply install all the required packages by npm install (if some packages not install by this then install individually)

## Then simple run the command npm start

## npm install
## npm start